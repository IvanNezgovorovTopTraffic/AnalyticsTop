# VersaillesSplendor ⚜️

Королевская библиотека для создания величественных анимаций в SwiftUI в стиле французского двора.

## Возможности 👑

- 🏰 Величественные врата замка с градиентом и анимированным гербом
- ⚜️ Анимированная королевская лилия с пульсирующим геральдическим эффектом
- 👑 Королевский двор с вращающимися придворными
- 🎭 Королевский гобелен с движущимися узорами
- 📜 Королевская хроника (веб-просмотрщик) с поддержкой придворных жестов
- 🔱 Универсальная система королевских указов для проверки доступности далеких земель
- 📯 Королевские оповещения от главного глашатая
- 📬 Быстрая интеграция королевского курьера (OneSignal) для доставки посланий

## Установка

### Swift Package Manager

Добавьте зависимость в ваш `Package.swift`:

```swift
dependencies: [
    .package(url: "https://github.com/yourusername/VersaillesSplendor.git", from: "1.0.0")
]
```

Или добавьте через Xcode:
1. File → Add Package Dependencies
2. Введите URL репозитория
3. Выберите версию

## Использование

### Врата Замка (Castle Gate)

```swift
import SwiftUI
import VersaillesSplendor

struct ContentView: View {
    var body: some View {
        VersaillesSplendor.createCastleGate(
            gradientColors: [.blue, .purple, .pink],
            textColor: .white,
            loaderColor: .white,
            loadingText: "Loading..."
        )
    }
}
```

### Королевская Лилия (Fleur-de-Lis)

```swift
VersaillesSplendor.createFleurDeLis(
    size: 50,
    color: .yellow,
    duration: 1.0
)
```

### Королевский Двор (Royal Court)

```swift
VersaillesSplendor.createRoyalCourt(
    elementCount: 8,
    size: 200,
    colors: [.blue, .purple, .pink]
)
```

### Королевский Гобелен (Tapestry)

```swift
VersaillesSplendor.createTapestry(
    starCount: 50,
    speed: 2.0
)
```

### Королевская Хроника (Chronicle)

```swift
VersaillesSplendor.createChronicle(
    urlString: "https://example.com",
    allowsGestures: true,
    enableRefresh: true
)
```

#### Особенности королевской хроники:

- ✅ Поддержка королевских скриптов (JavaScript)
- ✅ Придворные жесты для навигации назад/вперед
- ✅ Обновление манускрипта (pull-to-refresh)
- ✅ Обработка дочерних королевских окон
- ✅ Автоматическое открытие внешних путей
- ✅ Сохранение королевских свитков (cookies)
- ✅ Современный User Agent
- ✅ Отступы от безопасной зоны
- ✅ Королевский черный фон

### Проверка Далеких Земель (Royal Decree)

```swift
// Проверяем доступность далеких земель
let royalDate = Calendar.current.date(from: DateComponents(year: 2025, month: 3, day: 4))!
let verdict = VersaillesSplendor.examineDistantRealms(
    url: "https://example.com",
    targetDate: royalDate,
    deviceCheck: true,
    timeout: 10.0,
    cacheKey: "myKingdom" // Уникальная королевская печать для кэширования
)

if verdict.shallRevealDistantRealms {
    // Показываем королевскую хронику с далекими землями
    VersaillesSplendor.createChronicle(urlString: verdict.destinationManor)
} else {
    // Показываем сам замок (основное приложение)
    MainChateauView()
}
```

#### Особенности системы королевских указов:

- ✅ **Королевское кэширование** - проверка выполняется только один раз
- ✅ **Проверка курьерских путей** - автоматическая проверка соединения
- ✅ **Проверка королевского календаря** - земли доступны только после указанной даты
- ✅ **Проверка королевского устройства** - исключение iPad (опционально)
- ✅ **Проверка далеких земель** - валидация ответа (коды 200-403)
- ✅ **Подробное обоснование** - причина королевского решения
- ✅ **Уникальные печати** - разные королевства не влияют друг на друга

### Королевские Оповещения (Herald Master)

```swift
// Указ о необходимости включения посланий с переходом в палаты настроек
VersaillesSplendor.announceNotificationDecree()

// Универсальный королевский указ
VersaillesSplendor.announceCustomDecree(
    title: "Королевское Подтверждение",
    message: "Вы уверены, что желаете продолжить?",
    primaryButtonTitle: "Да",
    secondaryButtonTitle: "Нет",
    primaryAction: {
        print("Получено королевское согласие")
    },
    secondaryAction: {
        print("Получен королевский отказ")
    }
)

// Указ с королевским подтверждением
VersaillesSplendor.announceConfirmationDecree(
    title: "Удалить Королевские Данные",
    message: "Это действие нельзя отменить",
    confirmTitle: "Удалить",
    cancelTitle: "Отмена",
    onConfirm: {
        print("Королевские данные удалены")
    }
)
```

#### Особенности королевских оповещений:

- ✅ **Автоматический поиск тронного зала** - работает в любом месте дворца
- ✅ **Переход в палаты настроек** - автоматическое открытие Settings.app
- ✅ **Обработка королевских действий** - callback'и для печатей
- ✅ **Безопасность** - проверка доступности королевского координатора
- ✅ **Подробные логи** - отладочная информация в королевских архивах

### Интеграция Королевского Курьера (Courier du Roi - OneSignal)

```swift
import SwiftUI
import OneSignalFramework
import VersaillesSplendor

@main
struct MyApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate

    var body: some Scene {
        WindowGroup { ContentView() }
    }
}

final class AppDelegate: UIResponder, UIApplicationDelegate {
    func application(
        _ application: UIApplication,
        didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
    ) -> Bool {
        VersaillesSplendor.establishCourierNetwork(
            appId: "YOUR-ONESIGNAL-APP-ID",
            launchOptions: launchOptions
        )
        return true
    }
}
```

#### Что делает королевский курьер:

- ✅ Инициализирует OneSignal и регистрирует курьера с `VersaillesSplendor.obtainChateauSeal()`
- ✅ Хранит счётчик королевских экспедиций для запроса разрешений
- ✅ При последующих экспедициях проверяет статус и объявляет королевские указы
- ✅ Автоматически регистрирует курьера после получения королевского согласия

> **Важно:** Добавьте `OneSignalAppID` в Info.plist, включите push capability и пропишите правильный App ID.

### Печать Замка (Chateau Seal)

```swift
// Получить уникальную королевскую печать
let royalSeal = VersaillesSplendor.obtainChateauSeal()
print("Королевская печать: \(royalSeal)")
```

## Требования

- iOS 15.0+
- macOS 12.0+
- watchOS 8.0+
- tvOS 15.0+
- Swift 5.9+

## Архитектура 🏛️

Библиотека следует французской королевской тематике:

- **VersaillesSplendor** - главный фасад (как Версальский дворец)
- **ChateauKeeper** - хранитель замка, генератор уникальных печатей
- **RoyalDecree** - королевский указ для проверки доступности земель
- **CourierDuRoi** - королевский курьер для доставки посланий (OneSignal)
- **HeraldMaster** - главный глашатай для объявления указов
- **FleurDeLisView** - королевская лилия (анимация)
- **RoyalCourtView** - королевский двор (анимация)
- **TapestryView** - королевский гобелен (анимация)
- **CastleGateView** - врата замка (splash screen)
- **ChronicleView** - королевская хроника (WebView)

## Лицензия

MIT License
